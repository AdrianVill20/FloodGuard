This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: **/*.py, **/*.ts
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
floodguard_api/floodguard_api/__init__.py
floodguard_api/floodguard_api/asgi.py
floodguard_api/floodguard_api/settings.py
floodguard_api/floodguard_api/urls.py
floodguard_api/floodguard_api/wsgi.py
floodguard_api/manage.py
floodguard_api/nlp/__init__.py
floodguard_api/nlp/admin.py
floodguard_api/nlp/apps.py
floodguard_api/nlp/classifier.py
floodguard_api/nlp/migrations/__init__.py
floodguard_api/nlp/models.py
floodguard_api/nlp/tests.py
floodguard_api/nlp/urls.py
floodguard_api/nlp/views.py
floodguard_ionic/capacitor.config.ts
floodguard_ionic/src/app/app-routing.module.ts
floodguard_ionic/src/app/app.component.spec.ts
floodguard_ionic/src/app/app.component.ts
floodguard_ionic/src/app/app.module.ts
floodguard_ionic/src/app/home/home-routing.module.ts
floodguard_ionic/src/app/home/home.module.ts
floodguard_ionic/src/app/home/home.page.spec.ts
floodguard_ionic/src/app/home/home.page.ts
floodguard_ionic/src/app/pages/dashboard/dashboard.module.ts
floodguard_ionic/src/app/pages/dashboard/dashboard.page.spec.ts
floodguard_ionic/src/app/pages/dashboard/dashboard.page.ts
floodguard_ionic/src/app/pages/report/report.module.ts
floodguard_ionic/src/app/pages/report/report.page.spec.ts
floodguard_ionic/src/app/pages/report/report.page.ts
floodguard_ionic/src/app/services/api.spec.ts
floodguard_ionic/src/app/services/api.ts
floodguard_ionic/src/environments/environment.prod.ts
floodguard_ionic/src/environments/environment.ts
floodguard_ionic/src/main.ts
floodguard_ionic/src/polyfills.ts
floodguard_ionic/src/test.ts
floodguard_ionic/src/zone-flags.ts
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="floodguard_api/floodguard_api/__init__.py">

</file>

<file path="floodguard_api/floodguard_api/asgi.py">
"""
ASGI config for floodguard_api project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'floodguard_api.settings')

application = get_asgi_application()
</file>

<file path="floodguard_api/floodguard_api/settings.py">
"""
Django settings for floodguard_api project.

Generated by 'django-admin startproject' using Django 6.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""

from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-*@0cu)fwt9*u=fu%ctj3dg+k-*&e7fj%gv-ecel-__qhbp93ge'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Third party
    'rest_framework',
    'corsheaders',
    # Our apps
    'nlp',
]

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',  # ADD THIS FIRST
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'floodguard_api.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'floodguard_api.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = 'static/'

# CORS — allows Ionic app to call this API
CORS_ALLOW_ALL_ORIGINS = True

# REST Framework
REST_FRAMEWORK = {
    'DEFAULT_RENDERER_CLASSES': [
        'rest_framework.renderers.JSONRenderer',
    ]
}
</file>

<file path="floodguard_api/floodguard_api/urls.py">
# ============================================================
# floodguard_api/urls.py
# FloodGuard ASEAN — Main URL Config
# ============================================================

from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/',  admin.site.urls),
    path('api/',    include('nlp.urls')),
]
</file>

<file path="floodguard_api/floodguard_api/wsgi.py">
"""
WSGI config for floodguard_api project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'floodguard_api.settings')

application = get_wsgi_application()
</file>

<file path="floodguard_api/manage.py">
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'floodguard_api.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
</file>

<file path="floodguard_api/nlp/__init__.py">

</file>

<file path="floodguard_api/nlp/admin.py">
from django.contrib import admin

# Register your models here.
</file>

<file path="floodguard_api/nlp/apps.py">
from django.apps import AppConfig


class NlpConfig(AppConfig):
    name = 'nlp'
</file>

<file path="floodguard_api/nlp/classifier.py">
# ============================================================
# nlp/classifier.py
# FloodGuard ASEAN — NLP Classifier
# Ported from Google Colab to Django
# ============================================================

import re

# ----------------------------------------------------------
# LOCATION DICTIONARY
# ----------------------------------------------------------
CEBU_LOCATIONS = [
    "Lahug", "Guadalupe", "Talamban", "Busay", "Banawa",
    "Pari-an", "Ermita", "Sambag", "Mabolo", "Apas",
    "Labangon", "Pardo", "Tisa", "Bulacao", "Inayawan",
    "Carbon", "Colon", "Fuente", "Escario", "Kalunasan",
    "Kinasang-an", "Luz", "Pahina", "Tejero", "Tinago",
    "Mandaue", "Lapu-Lapu", "Talisay",
    "Consolacion", "Liloan", "Cordova",
    "Guar", "Tal", "Lahog", "Mandue",
]

LOCATION_SET = {loc.lower(): loc for loc in CEBU_LOCATIONS}

# ----------------------------------------------------------
# URGENCY KEYWORDS
# ----------------------------------------------------------
HIGH_KEYWORDS = [
    "baha", "lubog", "tabang", "rescue", "sos",
    "nalunod", "patay", "liki",
    "tulong", "saklolo", "lunod", "delikado", "naiipit",
    "help", "drowning", "trapped", "emergency",
    "critical", "danger", "urgent",
]

MEDIUM_KEYWORDS = [
    "umaapaw", "mataas", "puno", "hapit",
    "papataas", "overflow", "flooding", "apas",
    "rising", "peligro",
]

MEDIUM_PHRASES = [
    "mataas ang tubig", "mataas na ang tubig",
    "water rising", "rising water", "road flooded",
    "lumalaki ang tubig", "puno na", "hapit na",
    "papataas pa",
]

LOW_KEYWORDS = [
    "ulan", "ambon", "basa", "tag-ulan", "gamay",
    "umuulan", "maulap", "rain", "wet", "drizzle",
    "possible", "tubig", "alert", "mag-amping",
    "mag-ingat",
]

# ----------------------------------------------------------
# LANGUAGE MARKERS
# ----------------------------------------------------------
CEBUANO_MARKERS = [
    "nga", "naa", "dili", "kay", "unya", "bitaw",
    "dapit", "unsay", "basta", "tabang", "apas",
    "lubog", "ulan", "ambon", "hapit",
]
TAGALOG_MARKERS = [
    "ang", "mga", "ay", "ito", "yung", "saan",
    "paano", "naman", "kaya", "po", "raw",
    "tulong", "lunod", "naiipit", "umuulan",
]
ENGLISH_MARKERS = [
    "the", "is", "are", "and", "flood", "water",
    "road", "help", "area", "rising", "danger",
    "please", "already", "street",
]

# ----------------------------------------------------------
# PREPROCESSOR
# ----------------------------------------------------------
def preprocess(text):
    text = text.lower()
    text = re.sub(r'[^a-z0-9\s]', ' ', text)
    return text.split()

# ----------------------------------------------------------
# LOCATION EXTRACTOR
# ----------------------------------------------------------
def extract_location(tokens):
    for token in tokens:
        if token in LOCATION_SET:
            return LOCATION_SET[token]
        for key, name in LOCATION_SET.items():
            if len(token) >= 4 and (
                token in key or key in token or
                sum(a == b for a, b in zip(token, key)) >= len(key) - 1
            ):
                return name
    return None

# ----------------------------------------------------------
# URGENCY CLASSIFIER
# ----------------------------------------------------------
def classify_urgency(tokens):
    score = 0
    text  = ' '.join(tokens)

    for token in tokens:
        if token in HIGH_KEYWORDS:
            score += 2.0
        elif token in MEDIUM_KEYWORDS:
            score += 0.5
        elif token in LOW_KEYWORDS:
            score += 0.2

    phrase_matched = False
    for phrase in MEDIUM_PHRASES:
        if phrase in text:
            score += 0.8
            phrase_matched = True

    high_hit = any(token in HIGH_KEYWORDS for token in tokens)

    if high_hit and score >= 2.0:
        return "HIGH"
    elif score >= 0.9 or phrase_matched:
        return "MEDIUM"
    else:
        return "LOW"

# ----------------------------------------------------------
# LANGUAGE DETECTOR
# ----------------------------------------------------------
def detect_language(tokens):
    ceb = sum(1   for t in tokens if t in CEBUANO_MARKERS)
    tag = sum(1   for t in tokens if t in TAGALOG_MARKERS)
    eng = sum(0.8 for t in tokens if t in ENGLISH_MARKERS)

    scores = {"Cebuano": ceb, "Tagalog": tag, "English": eng}
    best   = max(scores, key=scores.get)

    if scores[best] == 0:
        return "Cebuano"
    return best

# ----------------------------------------------------------
# ALERT GENERATOR
# ----------------------------------------------------------
def generate_alert(location, urgency, language):
    loc = location or "your area"

    templates = {
        "HIGH": {
            "Cebuano": f"⚠️ ALERTO SA BAHA: Grabe nga baha sa {loc}. Palihog adto dayon sa pinakalapit nga evacuation center.",
            "Tagalog": f"⚠️ BABALA SA BAHA: Malubhang pagbaha sa {loc}. Mangyaring lumikas na agad sa pinakamalapit na evacuation center.",
            "English": f"⚠️ FLOOD ALERT: Severe flooding in {loc}. Please evacuate immediately to the nearest evacuation center.",
        },
        "MEDIUM": {
            "Cebuano": f"⚠️ PASIDAAN: Nagtubo ang tubig sa {loc}. Pag-andam ug posibleng pagbakwit.",
            "Tagalog": f"⚠️ BABALA: Tumataas ang tubig sa {loc}. Maghanda para sa posibleng paglikas.",
            "English": f"⚠️ FLOOD WARNING: Water rising in {loc}. Prepare for possible evacuation.",
        },
        "LOW": {
            "Cebuano": f"ℹ️ ABISO: Posibleng gamay nga baha sa {loc}. Pagbantay ug sunda ang mga updates.",
            "Tagalog": f"ℹ️ ABISO: Posibleng magbaha sa {loc}. Manatiling alerto at subaybayan ang mga update.",
            "English": f"ℹ️ ADVISORY: Light flooding possible in {loc}. Stay alert and monitor local updates.",
        },
    }

    return templates[urgency][language]

# ----------------------------------------------------------
# MAIN CLASSIFY FUNCTION
# ----------------------------------------------------------
def classify_message(text):
    tokens   = preprocess(text)
    location = extract_location(tokens)
    urgency  = classify_urgency(tokens)
    language = detect_language(tokens)
    alert    = generate_alert(location, urgency, language)

    return {
        "message" : text,
        "location": location,
        "urgency" : urgency,
        "language": language,
        "alert"   : alert,
    }
</file>

<file path="floodguard_api/nlp/migrations/__init__.py">

</file>

<file path="floodguard_api/nlp/models.py">
from django.db import models

# Create your models here.
</file>

<file path="floodguard_api/nlp/tests.py">
from django.test import TestCase

# Create your tests here.
</file>

<file path="floodguard_ionic/capacitor.config.ts">
import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'floodguard_ionic',
  webDir: 'www'
};

export default config;
</file>

<file path="floodguard_ionic/src/app/app.component.spec.ts">
import { CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { TestBed } from '@angular/core/testing';

import { AppComponent } from './app.component';

describe('AppComponent', () => {

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [AppComponent],
      schemas: [CUSTOM_ELEMENTS_SCHEMA],
    }).compileComponents();
  });

  it('should create the app', () => {
    const fixture = TestBed.createComponent(AppComponent);
    const app = fixture.componentInstance;
    expect(app).toBeTruthy();
  });

});
</file>

<file path="floodguard_ionic/src/app/app.component.ts">
import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: 'app.component.html',
  styleUrls: ['app.component.scss'],
  standalone: false,
})
export class AppComponent {
  constructor() {}
}
</file>

<file path="floodguard_ionic/src/app/app.module.ts">
import { NgModule } from '@angular/core';
import { BrowserModule } from '@angular/platform-browser';
import { RouteReuseStrategy } from '@angular/router';
import { HttpClientModule } from '@angular/common/http';

import { IonicModule, IonicRouteStrategy } from '@ionic/angular';

import { AppComponent } from './app.component';
import { AppRoutingModule } from './app-routing.module';

@NgModule({
  declarations: [AppComponent],
  imports: [
    BrowserModule,
    HttpClientModule,
    IonicModule.forRoot(),
    AppRoutingModule
  ],
  providers: [{ provide: RouteReuseStrategy, useClass: IonicRouteStrategy }],
  bootstrap: [AppComponent],
})
export class AppModule {}
</file>

<file path="floodguard_ionic/src/app/home/home-routing.module.ts">
import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';
import { HomePage } from './home.page';

const routes: Routes = [
  {
    path: '',
    component: HomePage,
  }
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class HomePageRoutingModule {}
</file>

<file path="floodguard_ionic/src/app/home/home.module.ts">
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { IonicModule } from '@ionic/angular';
import { FormsModule } from '@angular/forms';
import { HomePage } from './home.page';

import { HomePageRoutingModule } from './home-routing.module';


@NgModule({
  imports: [
    CommonModule,
    FormsModule,
    IonicModule,
    HomePageRoutingModule
  ],
  declarations: [HomePage]
})
export class HomePageModule {}
</file>

<file path="floodguard_ionic/src/app/home/home.page.spec.ts">
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { IonicModule } from '@ionic/angular';

import { HomePage } from './home.page';

describe('HomePage', () => {
  let component: HomePage;
  let fixture: ComponentFixture<HomePage>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [HomePage],
      imports: [IonicModule.forRoot()]
    }).compileComponents();

    fixture = TestBed.createComponent(HomePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
</file>

<file path="floodguard_ionic/src/app/home/home.page.ts">
import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  templateUrl: 'home.page.html',
  styleUrls: ['home.page.scss'],
  standalone: false,
})
export class HomePage {

  constructor() {}

}
</file>

<file path="floodguard_ionic/src/app/pages/dashboard/dashboard.module.ts">
import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { DashboardPage } from './dashboard.page';

@NgModule({
  imports: [
    DashboardPage,
    RouterModule.forChild([{ path: '', component: DashboardPage }])
  ]
})
export class DashboardPageModule {}
</file>

<file path="floodguard_ionic/src/app/pages/dashboard/dashboard.page.spec.ts">
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DashboardPage } from './dashboard.page';

describe('DashboardPage', () => {
  let component: DashboardPage;
  let fixture: ComponentFixture<DashboardPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DashboardPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
</file>

<file path="floodguard_ionic/src/app/pages/report/report.module.ts">
import { NgModule } from '@angular/core';
import { RouterModule } from '@angular/router';
import { ReportPage } from './report.page';

@NgModule({
  imports: [
    ReportPage,
    RouterModule.forChild([{ path: '', component: ReportPage }])
  ]
})
export class ReportPageModule {}
</file>

<file path="floodguard_ionic/src/app/pages/report/report.page.spec.ts">
import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReportPage } from './report.page';

describe('ReportPage', () => {
  let component: ReportPage;
  let fixture: ComponentFixture<ReportPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReportPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
</file>

<file path="floodguard_ionic/src/app/pages/report/report.page.ts">
// ============================================================
// src/app/pages/report/report.page.ts
// ============================================================

import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { IonicModule } from '@ionic/angular';
import { RouterModule } from '@angular/router';
import { ApiService } from '../../services/api';

@Component({
  selector: 'app-report',
  templateUrl: './report.page.html',
  styleUrls: ['./report.page.scss'],
  standalone: true,
  imports: [CommonModule, FormsModule, IonicModule, RouterModule]
})
export class ReportPage {

  message  : string  = '';
  isLoading: boolean = false;
  result   : any     = null;
  errorMsg : string  = '';

  constructor(private api: ApiService) {}

  submitReport() {
    if (!this.message.trim()) {
      this.errorMsg = 'Please enter a message.';
      return;
    }
    this.isLoading = true;
    this.result    = null;
    this.errorMsg  = '';

    this.api.classifyMessage(this.message).subscribe({
      next: (response) => {
        this.result    = response.data;
        this.isLoading = false;
      },
      error: () => {
        this.errorMsg  = 'Could not connect to server. Is Django running?';
        this.isLoading = false;
      }
    });
  }

  getUrgencyColor(): string {
    if (!this.result) return 'primary';
    if (this.result.urgency === 'HIGH')   return 'danger';
    if (this.result.urgency === 'MEDIUM') return 'warning';
    return 'success';
  }

  clearForm() {
    this.message = '';
    this.result  = null;
    this.errorMsg = '';
  }
}
</file>

<file path="floodguard_ionic/src/app/services/api.spec.ts">
import { TestBed } from '@angular/core/testing';

import { Api } from './api';

describe('Api', () => {
  let service: Api;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Api);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
</file>

<file path="floodguard_ionic/src/environments/environment.prod.ts">
export const environment = {
  production: true
};
</file>

<file path="floodguard_ionic/src/environments/environment.ts">
// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
</file>

<file path="floodguard_ionic/src/main.ts">
import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

import { AppModule } from './app/app.module';

platformBrowserDynamic().bootstrapModule(AppModule)
  .catch(err => console.log(err));
</file>

<file path="floodguard_ionic/src/polyfills.ts">
/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * You can add your own extra polyfills to this file.
 *
 * This file is divided into 2 sections:
 *   1. Browser polyfills. These are applied before loading ZoneJS and are sorted by browsers.
 *   2. Application imports. Files imported after ZoneJS that should be loaded before your main
 *      file.
 *
 * The current setup is for so-called "evergreen" browsers; the last versions of browsers that
 * automatically update themselves. This includes recent versions of Safari, Chrome (including
 * Opera), Edge on the desktop, and iOS and Chrome on mobile.
 *
 * Learn more in https://angular.io/guide/browser-support
 */

/***************************************************************************************************
 * BROWSER POLYFILLS
 */

/**
 * By default, zone.js will patch all possible macroTask and DomEvents
 * user can disable parts of macroTask/DomEvents patch by setting following flags
 * because those flags need to be set before `zone.js` being loaded, and webpack
 * will put import in the top of bundle, so user need to create a separate file
 * in this directory (for example: zone-flags.ts), and put the following flags
 * into that file, and then add the following code before importing zone.js.
 * import './zone-flags';
 *
 * The flags allowed in zone-flags.ts are listed here.
 *
 * The following flags will work for all browsers.
 *
 * (window as any).__Zone_disable_requestAnimationFrame = true; // disable patch requestAnimationFrame
 * (window as any).__Zone_disable_on_property = true; // disable patch onProperty such as onclick
 * (window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove']; // disable patch specified eventNames
 *
 *  in IE/Edge developer tools, the addEventListener will also be wrapped by zone.js
 *  with the following flag, it will bypass `zone.js` patch for IE/Edge
 *
 *  (window as any).__Zone_enable_cross_context_check = true;
 *
 */
 
import './zone-flags';

/***************************************************************************************************
 * Zone JS is required by default for Angular itself.
 */
import 'zone.js';  // Included with Angular CLI.


/***************************************************************************************************
 * APPLICATION IMPORTS
 */
</file>

<file path="floodguard_ionic/src/test.ts">
// This file is required by karma.conf.js and loads recursively all the .spec and framework files

import 'zone.js/testing';
import { getTestBed } from '@angular/core/testing';
import {
  BrowserDynamicTestingModule,
  platformBrowserDynamicTesting
} from '@angular/platform-browser-dynamic/testing';

// First, initialize the Angular testing environment.
getTestBed().initTestEnvironment(
  BrowserDynamicTestingModule,
  platformBrowserDynamicTesting(),
);
</file>

<file path="floodguard_ionic/src/zone-flags.ts">
/**
 * Prevents Angular change detection from
 * running with certain Web Component callbacks
 */
// eslint-disable-next-line no-underscore-dangle
(window as any).__Zone_disable_customElements = true;
</file>

<file path="floodguard_ionic/src/app/app-routing.module.ts">
import { NgModule } from '@angular/core';
import { PreloadAllModules, RouterModule, Routes } from '@angular/router';

const routes: Routes = [
  {
    path: '',
    redirectTo: 'dashboard',
    pathMatch: 'full'
  },
  // {
  //   path: 'report',
  //   loadChildren: () => import('./pages/report/report.module').then( m => m.ReportPageModule)
  // },
  {
    path: 'dashboard',
    loadChildren: () => import('./pages/dashboard/dashboard.module').then( m => m.DashboardPageModule)
  },
];

@NgModule({
  imports: [
    RouterModule.forRoot(routes, { preloadingStrategy: PreloadAllModules })
  ],
  exports: [RouterModule]
})
export class AppRoutingModule { }
</file>

<file path="floodguard_api/nlp/urls.py">
# ============================================================
# nlp/urls.py
# FloodGuard ASEAN — NLP URL Routing
# ============================================================

from django.urls import path
from . import views

urlpatterns = [
    path('ping/',                           views.ping,                  name='ping'),
    path('classify/',                       views.classify,              name='classify'),
    path('classify/batch/',                 views.classify_batch,        name='classify_batch'),
    path('barangays/',                      views.get_barangays,         name='barangays'),
    path('barangays/map/',                  views.get_barangay_map,      name='barangay_map'),
    path('barangays/pixels/<str:barangay_name>/<int:year>/',
         views.get_barangay_pixels,         name='barangay_pixels'),
    path('barangays/timeseries/<str:barangay_name>/',
         views.get_barangay_timeseries,     name='barangay_timeseries'),
    path('barangays/timeseries/all/<int:year>/',
         views.get_year_snapshot,           name='year_snapshot'),
]
</file>

<file path="floodguard_api/nlp/views.py">
# ============================================================
# nlp/views.py
# FloodGuard ASEAN — API Endpoints
# ============================================================
import json
import os
from django.conf import settings

from rest_framework.decorators import api_view
from rest_framework.response import Response
from rest_framework import status
from .classifier import classify_message

# ----------------------------------------------------------
# ENDPOINT 1: Health Check
# GET /api/ping
# ----------------------------------------------------------
@api_view(['GET'])
def ping(request):
    return Response({
        "status" : "ok",
        "message": "FloodGuard ASEAN NLP API is running",
        "version": "1.0"
    })

# ----------------------------------------------------------
# ENDPOINT 2: Classify single message
# POST /api/classify
# Body: { "message": "Baha na sa Lahug tabang" }
# ----------------------------------------------------------
@api_view(['POST'])
def classify(request):
    try:
        message = request.data.get('message', '').strip()

        if not message:
            return Response(
                {"error": "No message provided"},
                status=status.HTTP_400_BAD_REQUEST
            )

        result = classify_message(message)

        return Response({
            "status" : "success",
            "data"   : result
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )

# ----------------------------------------------------------
# ENDPOINT 3: Batch classify multiple messages
# POST /api/classify/batch
# Body: { "messages": ["msg1", "msg2", ...] }
# ----------------------------------------------------------
@api_view(['POST'])
def classify_batch(request):
    try:
        messages = request.data.get('messages', [])

        if not messages:
            return Response(
                {"error": "No messages provided"},
                status=status.HTTP_400_BAD_REQUEST
            )

        results = [classify_message(msg) for msg in messages]

        high   = sum(1 for r in results if r['urgency'] == 'HIGH')
        medium = sum(1 for r in results if r['urgency'] == 'MEDIUM')
        low    = sum(1 for r in results if r['urgency'] == 'LOW')

        return Response({
            "status" : "success",
            "total"  : len(results),
            "summary": {
                "HIGH"  : high,
                "MEDIUM": medium,
                "LOW"   : low
            },
            "data": results
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )
    

# ----------------------------------------------------------
# ENDPOINT 4: Get all barangay flood risk data
# GET /api/barangays/
# ----------------------------------------------------------
@api_view(['GET'])
def get_barangays(request):
    try:
        json_path = os.path.join(
            settings.BASE_DIR, 'static', 'data',
            'cebu_barangay_flood_risk.json'
        )
        with open(json_path, 'r') as f:
            data = json.load(f)

        # Summary counts
        high     = sum(1 for b in data if b['risk_level'] == 'HIGH')
        moderate = sum(1 for b in data if b['risk_level'] == 'MODERATE')
        low      = sum(1 for b in data if b['risk_level'] == 'LOW')

        return Response({
            "status" : "success",
            "total"  : len(data),
            "summary": {
                "HIGH"    : high,
                "MODERATE": moderate,
                "LOW"     : low
            },
            "data": data
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )

# ----------------------------------------------------------
# ENDPOINT 5: Get barangay GeoJSON for map
# GET /api/barangays/map/
# ----------------------------------------------------------
@api_view(['GET'])
def get_barangay_map(request):
    try:
        geojson_path = os.path.join(
            settings.BASE_DIR, 'static', 'data',
            'cebu_barangay_map.geojson'
        )
        with open(geojson_path, 'r') as f:
            data = json.load(f)

        return Response(data)

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )
    
# ----------------------------------------------------------
# ENDPOINT 6: Get pixel data for sub-barangay heatmap (year-aware)
# GET /api/barangays/pixels/<barangay_name>/<year>/
# ----------------------------------------------------------
@api_view(['GET'])
def get_barangay_pixels(request, barangay_name, year):
    try:
        pixels_path = os.path.join(
            settings.BASE_DIR, 'static', 'data',
            'cebu_barangay_pixels.json'
        )
        with open(pixels_path, 'r') as f:
            all_pixels = json.load(f)

        # Find the barangay (case-insensitive)
        matched_key = None
        for key in all_pixels.keys():
            if key.lower() == barangay_name.lower():
                matched_key = key
                break

        if not matched_key:
            return Response(
                {"error": f"Barangay '{barangay_name}' not found"},
                status=status.HTTP_404_NOT_FOUND
            )

        pixels = all_pixels[matched_key]

        # Load timeseries data to apply year-specific flood risk values
        try:
            timeseries_path = os.path.join(
                settings.BASE_DIR, 'static', 'data',
                'cebu_barangay_timeseries.json'
            )
            with open(timeseries_path, 'r') as f:
                timeseries_data = json.load(f)
            
            year_str = str(year)
            if matched_key in timeseries_data and year_str in timeseries_data[matched_key]:
                year_entry = timeseries_data[matched_key][year_str]
                year_flood_risk = year_entry.get('flood_risk')
                year_ndvi = year_entry.get('ndvi')
                
                # Apply year-specific values to pixels
                if year_flood_risk is not None or year_ndvi is not None:
                    pixels = [dict(p) for p in pixels]  # Deep copy to avoid modifying original
                    for pixel in pixels:
                        if year_flood_risk is not None:
                            pixel['flood'] = year_flood_risk
                        if year_ndvi is not None:
                            pixel['ndvi'] = year_ndvi
        except Exception as e:
            # If timeseries loading fails, just use static pixel data
            pass

        return Response({
            "status"  : "success",
            "barangay": matched_key,
            "year"    : year,
            "total"   : len(pixels),
            "pixels"  : pixels
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )
# ----------------------------------------------------------
# ENDPOINT 7: Get time series data for a barangay
# GET /api/barangays/timeseries/<barangay_name>/
# ----------------------------------------------------------
@api_view(['GET'])
def get_barangay_timeseries(request, barangay_name):
    try:
        path = os.path.join(
            settings.BASE_DIR, 'static', 'data',
            'cebu_barangay_timeseries.json'
        )
        with open(path, 'r') as f:
            all_data = json.load(f)

        # Case-insensitive match
        matched_key = None
        for key in all_data.keys():
            if key.lower() == barangay_name.lower():
                matched_key = key
                break

        if not matched_key:
            return Response(
                {"error": f"Barangay '{barangay_name}' not found"},
                status=status.HTTP_404_NOT_FOUND
            )

        data = all_data[matched_key]

        # Format for chart
        years      = sorted(data.keys())
        ndvi_series = []
        risk_series = []

        for year in years:
            ndvi_series.append({
                "year"   : int(year),
                "value"  : data[year]['ndvi'],
                "partial": data[year]['partial']
            })
            risk_series.append({
                "year"   : int(year),
                "value"  : data[year]['flood_risk'],
                "partial": data[year]['partial']
            })

        return Response({
            "status"    : "success",
            "barangay"  : matched_key,
            "ndvi"      : ndvi_series,
            "flood_risk": risk_series,
            "years"     : [int(y) for y in years],
            "note"      : "2026 data is partial (Jan-Apr only)"
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )

# ----------------------------------------------------------
# ENDPOINT 8: Get all barangays time series (for map slider)
# GET /api/barangays/timeseries/all/<year>/
# ----------------------------------------------------------
@api_view(['GET'])
def get_year_snapshot(request, year):
    try:
        path = os.path.join(
            settings.BASE_DIR, 'static', 'data',
            'cebu_barangay_timeseries.json'
        )
        with open(path, 'r') as f:
            all_data = json.load(f)

        year_str  = str(year)
        snapshot  = []

        for barangay, years in all_data.items():
            if year_str in years:
                entry = years[year_str]
                flood = entry.get('flood_risk', None)

                if flood is not None:
                    if flood >= 0.70:
                        risk_level = "HIGH"
                    elif flood >= 0.50:
                        risk_level = "MODERATE"
                    else:
                        risk_level = "LOW"
                else:
                    risk_level = "UNKNOWN"

                snapshot.append({
                    "barangay"  : barangay,
                    "year"      : year,
                    "ndvi"      : entry.get('ndvi'),
                    "flood_risk": flood,
                    "risk_level": risk_level,
                    "partial"   : entry.get('partial', False)
                })

        return Response({
            "status"  : "success",
            "year"    : year,
            "total"   : len(snapshot),
            "data"    : snapshot
        })

    except Exception as e:
        return Response(
            {"error": str(e)},
            status=status.HTTP_500_INTERNAL_SERVER_ERROR
        )
</file>

<file path="floodguard_ionic/src/app/services/api.ts">
// ============================================================
// src/app/services/api.ts
// FloodGuard ASEAN — Django API Connection
// ============================================================

import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class ApiService {

  private baseUrl = 'http://127.0.0.1:8000/api';

  constructor(private http: HttpClient) {}

  // Health check
  ping(): Observable<any> {
    return this.http.get(`${this.baseUrl}/ping/`);
  }

  // Classify single message
  classifyMessage(message: string): Observable<any> {
    return this.http.post(`${this.baseUrl}/classify/`, { message });
  }

  // Classify batch messages
  classifyBatch(messages: string[]): Observable<any> {
    return this.http.post(`${this.baseUrl}/classify/batch/`, { messages });
  }

  // Get all barangay flood risk data
  getBarangays(): Observable<any> {
    return this.http.get(`${this.baseUrl}/barangays/`);
  }

  // Get barangay GeoJSON for map
  getBarangayMap(): Observable<any> {
    return this.http.get(`${this.baseUrl}/barangays/map/`);
  }

  // Get pixel data for sub-barangay heatmap for a specific year
  // URL: /api/barangays/pixels/<barangay_name>/<year>/
  getBarangayPixels(barangayName: string, year: number): Observable<any> {
    const encoded = encodeURIComponent(barangayName);
    return this.http.get(`${this.baseUrl}/barangays/pixels/${encoded}/${year}/`);
  }

  // Get time series for one barangay
  getBarangayTimeseries(barangayName: string): Observable<any> {
    const encoded = encodeURIComponent(barangayName);
    return this.http.get(`${this.baseUrl}/barangays/timeseries/${encoded}/`);
  }

  // Get all barangays snapshot for a specific year (map slider)
  getYearSnapshot(year: number): Observable<any> {
    return this.http.get(`${this.baseUrl}/barangays/timeseries/all/${year}/`);
  }
}
</file>

<file path="floodguard_ionic/src/app/pages/dashboard/dashboard.page.ts">
// ============================================================
// src/app/pages/dashboard/dashboard.page.ts
// FloodGuard ASEAN — Full Dashboard with Timeline Slider
// ============================================================
import { Component, OnInit, AfterViewInit } from '@angular/core';
import { CommonModule } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { IonicModule } from '@ionic/angular';
import { RouterModule } from '@angular/router';
import { ApiService } from '../../services/api';
import * as L from 'leaflet';
import 'leaflet.heat';

@Component({
  selector: 'app-dashboard',
  templateUrl: './dashboard.page.html',
  styleUrls: ['./dashboard.page.scss'],
  standalone: true,
  imports: [CommonModule, FormsModule, IonicModule, RouterModule]
})
export class DashboardPage implements OnInit, AfterViewInit {
  // --- State ---
  barangays        : any[]   = [];
  filteredBarangays: any[]   = [];
  searchQuery      : string  = '';
  isLoading        : boolean = false;
  summary          : any     = { HIGH: 0, MODERATE: 0, LOW: 0 };
  selectedTab      : string  = 'map';
  activeLayer      : string  = 'flood';
  selectedBarangay : string  = '';
  selectedYear     : number  = 2020;
  sliderYear       : number  = 2020;
  isSliderLoading  : boolean = false;

  // Table slider state
  tableYear           : number  = 2020;
  isTableSliderLoading: boolean = false;
  private tableYearLookup: any  = {};

  // Toggles
  heatmapVisible   : boolean = true;
  showBoundaries   : boolean = true;

  // Table sorting
  sortColumn       : string  = 'barangay';
  sortDirection    : string  = 'asc'; // 'asc' or 'desc'

  // Chart data
  chartData        : any     = null;
  showChart        : boolean = false;

  // Reports
  reports          : any[]   = [];
  reportLoading    : boolean = false;

  // Map
  private map               : any;
  private geojsonLayer      : any;
  private heatLayer         : any;
  private clickOverlayGroup : any; // Layer group for invisible interactive circles
  private currentPixels     : any[] = [];
  private yearLayers        : any   = {};

  // Pending heatmap subscription
  private heatmapSub   : any   = null;

  // Year slider config
  years        = [2010, 2011, 2012, 2013, 2014, 2015, 2016,
                  2017, 2018, 2019, 2020, 2021, 2022, 2023,
                  2024, 2025, 2026];
  partialYears = [2026];

  sampleMessages = [
    "Baha na sa Lahug tabang!",
    "Mataas ang tubig sa Talisay papataas pa",
    "Ulan na sa Busay gamay pa",
    "Naiipit kami sa Ermita baha emergency",
    "Water rising in Lapu-Lapu road flooded",
    "Ambon sa Talamban basa ang dalan",
  ];

  constructor(private api: ApiService) {}

  ngOnInit() {
    this.loadBarangays();
    this.loadReports();
  }

  ngAfterViewInit() {
    setTimeout(() => this.initMap(), 500);
  }

  ionViewWillEnter() {
    this.loadBarangays();
  }

  handleRefresh(event: any) {
    this.loadBarangays();
    this.loadReports();
    setTimeout(() => event.target.complete(), 1500);
  }

  // --- Load barangay list ---
  loadBarangays() {
    this.isLoading = true;
    this.api.getBarangays().subscribe({
      next: (response: any) => {
        this.barangays = response.data.sort(
          (a: any, b: any) => b.flood_risk - a.flood_risk
        );
        this.summary   = response.summary;
        this.isLoading = false;
        this.loadTableYearSnapshot(this.tableYear);
      },
      error: () => { this.isLoading = false; }
    });
  }

  // --- Load year snapshot data for the TABLE slider ---
  loadTableYearSnapshot(year: number) {
    this.isTableSliderLoading = true;
    this.api.getYearSnapshot(year).subscribe({
      next: (response: any) => {
        this.tableYearLookup = {};
        response.data.forEach((entry: any) => {
          this.tableYearLookup[entry.barangay] = entry;
        });
        this.barangays = this.barangays.map((b: any) => {
          const snapshot = this.tableYearLookup[b.barangay];
          return {
            ...b,
            ndvi      : snapshot ? snapshot.ndvi       : null,
            risk_level: snapshot ? snapshot.risk_level : b.risk_level,
            flood_risk: snapshot ? snapshot.flood_risk : b.flood_risk,
          };
        });
        this.applySearch();
        this.isTableSliderLoading = false;
      },
      error: () => { this.isTableSliderLoading = false; }
    });
  }

  // --- Table slider change handler ---
  onTableSliderChange(event: any) {
    this.tableYear = parseInt(event.detail.value);
    this.loadTableYearSnapshot(this.tableYear);
  }

  // --- Search fix ---
  applySearch() {
    const query = this.searchQuery.toLowerCase().trim();
    if (!query) {
      this.filteredBarangays = this.barangays;
      return;
    }
    this.filteredBarangays = this.barangays.filter(b =>
      b.barangay.toLowerCase().includes(query) ||
      b.risk_level.toLowerCase().includes(query)
    );
  }

  searchBarangays(event: any) {
    this.searchQuery = (event.target?.value ?? event.detail?.value ?? '').toLowerCase().trim();
    this.applySearch();
  }

  // --- Dynamic deforestation status from NDVI ---
  getDeforestationStatus(ndvi: number | null | undefined): string {
    if (ndvi === null || ndvi === undefined) return 'Unknown';
    if (ndvi < 0.2)  return 'Severely Deforested';
    if (ndvi < 0.4)  return 'Mildly Deforested';
    return 'Healthy';
  }

  getDeforestationChipClass(ndvi: number | null | undefined): string {
    if (ndvi === null || ndvi === undefined) return '';
    if (ndvi < 0.2)  return 'fg-chip--danger';
    if (ndvi < 0.4)  return 'fg-chip--warning';
    return 'fg-chip--success';
  }

  // --- Toggle heatmap visibility ---
  toggleHeatmap() {
    this.heatmapVisible = !this.heatmapVisible;
    if (!this.map) return;

    if (this.heatmapVisible) {
      this.renderHeatmap(this.activeLayer);
    } else {
      if (this.heatLayer) this.map.removeLayer(this.heatLayer);
      if (this.clickOverlayGroup) this.clickOverlayGroup.clearLayers();
    }
  }

  // --- Toggle boundaries visibility ---
  toggleBoundaries() {
    this.showBoundaries = !this.showBoundaries;
    if (!this.map) return;

    const currentGeoJsonLayer = this.yearLayers[this.selectedYear];
    if (currentGeoJsonLayer) {
      if (this.showBoundaries) {
        currentGeoJsonLayer.addTo(this.map);
      } else {
        this.map.removeLayer(currentGeoJsonLayer);
      }
    }
  }

  // --- Click barangay from list → show on map ---
  selectBarangayFromList(barangay: any) {
    this.loadBarangayChart(barangay.barangay);
    this.selectedBarangay = barangay.barangay;
    this.selectedTab      = 'map';
    setTimeout(() => {
      if (!this.map) this.initMap();
      this.map.invalidateSize();
      setTimeout(() => {
        this.api.getBarangayMap().subscribe({
          next: (geojson: any) => {
            const feature = geojson.features.find(
              (f: any) => f.properties.barangay === barangay.barangay
            );
            if (!feature) {
              console.warn('Feature not found:', barangay.barangay);
              return;
            }
            const tempLayer = L.geoJSON(feature);
            const bounds    = tempLayer.getBounds();
            this.map.invalidateSize();
            this.map.fitBounds(bounds, { padding: [40, 40] });
            const highlight = L.geoJSON(feature, {
              style: {
                fillColor  : '#1a56a0',
                fillOpacity: 0.5,
                color      : '#1a56a0',
                weight     : 3
              }
            }).addTo(this.map);
            setTimeout(() => {
              this.map.removeLayer(highlight);
              this.loadBarangayHeatmap(barangay.barangay);
            }, 2000);
          }
        });
      }, 800);
    }, 800);
  }

  // --- Load reports ---
  loadReports() {
    this.reportLoading = true;
    this.api.classifyBatch(this.sampleMessages).subscribe({
      next: (response: any) => {
        this.reports       = response.data;
        this.reportLoading = false;
      },
      error: () => { this.reportLoading = false; }
    });
  }

  // --- Init map ---
  initMap() {
    if (this.map) return;

    const standardMap = L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '© OpenStreetMap contributors'
    });

    const topoMap = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Topo_Map/MapServer/tile/{z}/{y}/{x}', {
      attribution: 'Tiles © Esri'
    });

    this.map = L.map('flood-map', {
      center: [10.3157, 123.8854],
      zoom  : 12,
      layers: [standardMap]
    });

    // Initialize the invisible click overlay group
    this.clickOverlayGroup = L.layerGroup().addTo(this.map);

    const baseMaps = {
      "Standard View" : standardMap,
      "Elevation View": topoMap
    };
    L.control.layers(baseMaps, undefined, { position: 'topright' }).addTo(this.map);

    this.loadYearSnapshot(this.selectedYear);
  }

  // --- Get color based on NDVI value (vegetation health) ---
  // Green (healthy) → Yellow (moderate) → Red (deforested)
  getNDVIColor(ndvi: number | null | undefined): string {
    if (ndvi === null || ndvi === undefined) return '#999';
    if (ndvi >= 0.40) return '#27ae60'; // Green — Healthy forest
    if (ndvi >= 0.30) return '#f1c40f'; // Yellow — Moderate vegetation
    if (ndvi >= 0.20) return '#e67e22'; // Orange — Some deforestation
    return '#e74c3c'; // Red — Severe deforestation
  }

  // --- Get color based on Flood Risk value ---
  // Green (low risk) → Yellow (moderate) → Red (high risk)
  getFloodRiskLevelColor(floodRisk: number | null | undefined): string {
    if (floodRisk === null || floodRisk === undefined) return '#999';
    if (floodRisk < 0.40) return '#27ae60'; // Green — Low risk
    if (floodRisk < 0.60) return '#f1c40f'; // Yellow — Moderate risk
    if (floodRisk < 0.75) return '#e67e22'; // Orange — High risk
    return '#e74c3c'; // Red — Critical risk
  }

  // --- Load year snapshot for MAP slider ---
  // Now dynamically colors based on NDVI or Flood Risk depending on activeLayer
  loadYearSnapshot(year: number) {
    this.isSliderLoading = true;
    this.selectedYear    = year;
    Object.values(this.yearLayers).forEach((layer: any) => {
      if (this.map.hasLayer(layer)) this.map.removeLayer(layer);
    });
    this.api.getYearSnapshot(year).subscribe({
      next: (response: any) => {
        const data = response.data;
        this.api.getBarangayMap().subscribe({
          next: (geojson: any) => {
            const lookup: any = {};
            data.forEach((b: any) => { lookup[b.barangay] = b; });
            const layer = L.geoJSON(geojson, {
              style: (feature: any) => {
                const name  = feature.properties.barangay;
                const entry = lookup[name];
                
                // Choose color based on active layer (NDVI or Flood Risk)
                let fillColor = '#999';
                if (entry) {
                  if (this.activeLayer === 'ndvi') {
                    fillColor = this.getNDVIColor(entry.ndvi);
                  } else {
                    fillColor = this.getFloodRiskLevelColor(entry.flood_risk);
                  }
                }
                
                return {
                  fillColor  : fillColor,
                  fillOpacity: 0.7,
                  color      : '#333',
                  weight     : 1
                };
              },
              onEachFeature: (feature: any, layer: any) => {
                const name  = feature.properties.barangay;
                const entry = lookup[name];
                const ndvi  = entry?.ndvi?.toFixed(4) || 'N/A';
                const risk  = entry?.flood_risk?.toFixed(4) || 'N/A';
                
                layer.bindPopup(`
                  <strong>${name}</strong><br/>
                  Year: <b>${year}${this.partialYears.includes(year) ? ' (partial)' : ''}</b><br/>
                  <span style="color:${this.getFloodRiskLevelColor(entry?.flood_risk)}">🌊 Flood Risk: ${risk}</span><br/>
                  <span style="color:${this.getNDVIColor(entry?.ndvi)}">🌳 NDVI: ${ndvi}</span>
                `, { autoPan: false });

                layer.on('click', (e: any) => {
                  L.DomEvent.stopPropagation(e);
                  this.loadBarangayChart(name);
                  this.loadBarangayHeatmap(name);
                });

                layer.on('mouseover', function(this: any) {
                  this.setStyle({ weight: 2, fillOpacity: 0.9 });
                });
                layer.on('mouseout', function(this: any) {
                  this.setStyle({ weight: 1, fillOpacity: 0.7 });
                });
              }
            });

            // Respect boundary toggle when adding the new layer
            if (this.showBoundaries) {
              layer.addTo(this.map);
            }

            this.yearLayers[year] = layer;
            this.isSliderLoading  = false;
          }
        });
      },
      error: () => { this.isSliderLoading = false; }
    });
  }

  // --- Map year slider change ---
  onSliderChange(event: any) {
    const year      = parseInt(event.detail.value);
    this.sliderYear = year;
    this.loadYearSnapshot(year);
    if (this.selectedBarangay) {
      this.loadBarangayHeatmap(this.selectedBarangay);
    }
  }

  // --- Load time series chart for barangay ---
  loadBarangayChart(barangayName: string) {
    this.selectedBarangay = barangayName;
    this.showChart        = false;
    this.api.getBarangayTimeseries(barangayName).subscribe({
      next: (response: any) => {
        this.chartData = response;
        this.showChart = true;
        this.drawChart(response);
      },
      error: (err: any) => console.error(err)
    });
  }

  // --- Draw SVG chart ---
  drawChart(data: any) {
    setTimeout(() => {
      const canvas = document.getElementById('timeseries-chart') as HTMLCanvasElement;
      if (!canvas) return;
      const ctx    = canvas.getContext('2d');
      if (!ctx) return;
      const w      = canvas.width  = canvas.offsetWidth;
      const h      = canvas.height = 200;
      const pad    = { top: 20, right: 20, bottom: 30, left: 40 };
      const chartW = w - pad.left - pad.right;
      const chartH = h - pad.top  - pad.bottom;
      ctx.clearRect(0, 0, w, h);
      const years    = data.years;
      const ndviVals = data.ndvi.map((d: any) => d.value || 0);
      const riskVals = data.flood_risk.map((d: any) => d.value || 0);
      const n        = years.length;
      const xPos = (i: number) => pad.left + (i / (n - 1)) * chartW;
      const yPos = (v: number) => pad.top  + (1 - v) * chartH;
      ctx.strokeStyle = '#eee';
      ctx.lineWidth   = 1;
      [0, 0.25, 0.5, 0.75, 1].forEach(v => {
        ctx.beginPath();
        ctx.moveTo(pad.left, yPos(v));
        ctx.lineTo(pad.left + chartW, yPos(v));
        ctx.stroke();
        ctx.fillStyle = '#999';
        ctx.font      = '10px Arial';
        ctx.fillText(v.toFixed(2), 2, yPos(v) + 4);
      });
      ctx.fillStyle = '#666';
      ctx.font      = '10px Arial';
      years.forEach((yr: number, i: number) => {
        const partial = data.ndvi[i]?.partial;
        ctx.fillText(yr + (partial ? '*' : ''), xPos(i) - 10, h - 5);
      });
      ctx.beginPath();
      ctx.strokeStyle = '#e74c3c';
      ctx.lineWidth   = 2.5;
      riskVals.forEach((v: number, i: number) => {
        i === 0 ? ctx.moveTo(xPos(i), yPos(v)) : ctx.lineTo(xPos(i), yPos(v));
      });
      ctx.stroke();
      ctx.beginPath();
      ctx.strokeStyle = '#27ae60';
      ctx.lineWidth   = 2.5;
      ndviVals.forEach((v: number, i: number) => {
        i === 0 ? ctx.moveTo(xPos(i), yPos(v)) : ctx.lineTo(xPos(i), yPos(v));
      });
      ctx.stroke();
      riskVals.forEach((v: number, i: number) => {
        ctx.beginPath();
        ctx.arc(xPos(i), yPos(v), 4, 0, Math.PI * 2);
        ctx.fillStyle = '#e74c3c';
        ctx.fill();
      });
      ndviVals.forEach((v: number, i: number) => {
        ctx.beginPath();
        ctx.arc(xPos(i), yPos(v), 4, 0, Math.PI * 2);
        ctx.fillStyle = '#27ae60';
        ctx.fill();
      });
    }, 100);
  }

  // --- Load heatmap pixels for a barangay at the current sliderYear ---
  loadBarangayHeatmap(barangayName: string) {
    if (this.heatmapSub) {
      this.heatmapSub.unsubscribe();
      this.heatmapSub = null;
    }

    if (this.heatLayer) {
      this.map.removeLayer(this.heatLayer);
      this.heatLayer = null;
    }

    if (this.clickOverlayGroup) {
      this.clickOverlayGroup.clearLayers();
    }

    this.currentPixels    = [];
    this.selectedBarangay = barangayName;

    this.heatmapSub = this.api.getBarangayPixels(barangayName, this.sliderYear).subscribe({
      next: (response: any) => {
        this.currentPixels = response.pixels;
        this.renderHeatmap(this.activeLayer);
        this.heatmapSub = null;
      },
      error: (err: any) => {
        console.error('Heatmap load error:', err);
        this.heatmapSub = null;
      }
    });
  }

  // --- Render heatmap dots and invisible overlay ---
  renderHeatmap(layerType: string) {
    if (this.heatLayer) {
      this.map.removeLayer(this.heatLayer);
      this.heatLayer = null;
    }

    if (this.clickOverlayGroup) {
      this.clickOverlayGroup.clearLayers();
    }

    if (!this.currentPixels.length) return;

    // --- Custom gradients ---
    // NDVI: dark green (healthy/low intensity) → red (barren/high intensity)
    const ndviGradient  = { 0.0: 'darkgreen', 0.3: 'yellow', 0.6: 'orange', 1.0: 'red' };
    // Flood: cyan (low risk) → purple (high risk)
    const floodGradient = { 0.2: 'cyan', 0.5: 'blue', 0.8: 'darkblue', 1.0: 'purple' };

    // --- NDVI: compute data-driven max from positive pixels only ---
    // We do NOT use a hardcoded ceiling like 0.8 because real pixel values may
    // cluster in 0.2–0.5, making everything look barren against a 0.8 scale.
    // Instead, find the actual max positive NDVI in this dataset, then use that
    // as the "fully healthy" anchor. Combined with max:1.0 on the heatLayer,
    // the gradient is still globally absolute but calibrated to real data range.
    const positiveNdviPixels = this.currentPixels
      .map((p: any) => (p.ndvi !== null && p.ndvi !== undefined) ? p.ndvi : 0)
      .filter((v: number) => v > 0);

    // Healthy anchor: use the 90th-percentile positive NDVI value so a few
    // outlier high pixels don't compress everything else toward "barren".
    // Falls back to 0.5 if there are no positive pixels at all.
    let ndviHealthyMax = 0.5;
    if (positiveNdviPixels.length > 0) {
      const sorted = [...positiveNdviPixels].sort((a, b) => a - b);
      const p90idx = Math.floor(sorted.length * 0.90);
      ndviHealthyMax = sorted[p90idx] ?? sorted[sorted.length - 1];
      // Enforce a minimum floor so the scale never collapses on sparse data
      ndviHealthyMax = Math.max(ndviHealthyMax, 0.3);
    }

    const heatData = this.currentPixels.map((p: any) => {
      let intensity = 0;

      if (layerType === 'ndvi') {
        const rawNdvi = (p.ndvi !== null && p.ndvi !== undefined) ? p.ndvi : 0;

        if (rawNdvi < 0) {
          // Water / cloud pixels: 0 intensity → dark green end, not red
          intensity = 0;
        } else {
          // Remap [0 … ndviHealthyMax] → intensity [1.0 … 0.0]
          // Anything at or above the healthy anchor maps to 0 (fully green).
          // Anything at 0 maps to 1.0 (fully red / barren).
          const clampedNdvi = Math.min(rawNdvi, ndviHealthyMax);
          intensity = 1.0 - (clampedNdvi / ndviHealthyMax);
          intensity = Math.max(0, Math.min(1.0, intensity));
        }

      } else if (layerType === 'flood') {
        // Backend may send flood risk as 0–100 (percentage) or 0.0–1.0.
        // Normalise to 0.0–1.0 regardless.
        let rawFlood = (p.flood !== null && p.flood !== undefined) ? p.flood : 0;
        if (rawFlood > 1.0) {
          rawFlood = rawFlood / 100.0;
        }
        intensity = Math.max(0, Math.min(1.0, rawFlood));
      }

      return [p.lat, p.lon, intensity];
    });

    const activeGradient = layerType === 'flood' ? floodGradient : ndviGradient;

    this.heatLayer = (L as any).heatLayer(heatData, {
      radius    : 20,
      blur      : 15,
      minOpacity: 0.6,
      maxZoom   : 16,
      // FIX: lock the gradient to an absolute global scale of 1.0.
      // Without this, Leaflet.heat autoscales to the highest value in the
      // current dataset, making every barangay look identically "severe".
      max       : 1.0,
      gradient  : activeGradient
    });

    if (this.heatmapVisible) {
      this.heatLayer.addTo(this.map);

      // Create invisible clickable overlay for pixel-level popups
      for (const p of this.currentPixels) {
        const circle = L.circleMarker([p.lat, p.lon], {
          radius     : 12,
          opacity    : 0,
          fillOpacity: 0,
          interactive: true
        });

        circle.bindPopup(`
          <strong>${this.selectedBarangay}</strong><br/>
          Year: <b>${this.sliderYear}</b><br/>
          NDVI: <b>${p.ndvi !== undefined && p.ndvi !== null ? p.ndvi.toFixed(4) : 'N/A'}</b><br/>
          Flood Risk: <b>${p.flood !== undefined && p.flood !== null ? p.flood.toFixed(4) : 'N/A'}</b><br/>
          Elevation: <b>${p.elev !== undefined && p.elev !== null ? p.elev + ' m' : 'N/A'}</b>
        `);

        this.clickOverlayGroup.addLayer(circle);
      }
    }
  }

  // --- Switch layer ---
  // Also refreshes map colors when switching between NDVI and Flood Risk
  switchLayer(layer: string) {
    this.activeLayer = layer;
    
    // Refresh heatmap for selected barangay
    if (this.selectedBarangay) {
      this.renderHeatmap(layer);
    }
    
    // Refresh map colors for current year
    if (this.map && this.yearLayers[this.selectedYear]) {
      this.loadYearSnapshot(this.selectedYear);
    }
  }

  // --- Table sorting ---
  sortTable(column: string) {
    if (this.sortColumn === column) {
      this.sortDirection = this.sortDirection === 'asc' ? 'desc' : 'asc';
    } else {
      this.sortColumn    = column;
      this.sortDirection = 'asc';
    }

    this.filteredBarangays.sort((a: any, b: any) => {
      let valA = a[column];
      let valB = b[column];

      if (typeof valA === 'string' && !isNaN(parseFloat(valA))) {
        valA = parseFloat(valA);
        valB = parseFloat(valB);
      }

      if (valA == null) valA = '';
      if (valB == null) valB = '';

      if (typeof valA === 'string') {
        valA = valA.toLowerCase();
        valB = valB.toLowerCase();
      }

      if (this.sortDirection === 'asc') {
        return valA < valB ? -1 : valA > valB ? 1 : 0;
      } else {
        return valA > valB ? -1 : valA < valB ? 1 : 0;
      }
    });
  }

  // --- Get sort indicator ---
  getSortIndicator(column: string): string {
    if (this.sortColumn !== column) return '';
    return this.sortDirection === 'asc' ? ' ↑' : ' ↓';
  }

  // --- Reset map ---
  resetMap() {
    this.selectedBarangay = '';
    this.currentPixels    = [];
    if (this.heatLayer) {
      this.map.removeLayer(this.heatLayer);
      this.heatLayer = null;
    }
    if (this.clickOverlayGroup) {
      this.clickOverlayGroup.clearLayers();
    }
    this.map.setView([10.3157, 123.8854], 12);
  }

  // --- Risk color helper ---
  getRiskColor(riskLevel: string): string {
    switch (riskLevel) {
      case 'HIGH'    : return '#e74c3c';
      case 'MODERATE': return '#e67e22';
      case 'LOW'     : return '#27ae60';
      default        : return '#95a5a6';
    }
  }

  // --- Switch tab ---
  switchTab(tab: string) {
    this.selectedTab = tab;
    if (tab === 'map') {
      setTimeout(() => {
        if (this.map) this.map.invalidateSize();
      }, 300);
    }
  }

  // --- Color helper for heatmap ---
  getHeatColor(value: number): string {
    if (value >= 0.75) return '#e74c3c';
    if (value >= 0.60) return '#e67e22';
    if (value >= 0.45) return '#f1c40f';
    if (value >= 0.30) return '#2ecc71';
    return '#27ae60';
  }

  // --- Urgency color helper ---
  getUrgencyColor(urgency: string): string {
    if (urgency === 'HIGH')   return 'danger';
    if (urgency === 'MEDIUM') return 'warning';
    return 'success';
  }

  // --- Risk badge color helper ---
  getRiskBadgeColor(risk: string): string {
    if (risk === 'HIGH')     return 'danger';
    if (risk === 'MODERATE') return 'warning';
    return 'success';
  }
}
</file>

</files>
